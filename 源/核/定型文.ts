/**
 * kawaiko の声で書いた在庫の一言。API 予算を使いたくない場面 — 利用制限、予算切れ、
 * 異常、拒否、ループ — のために用意してある。
 *
 * 文面は 定型文.json にある。コードを触らずに調整できるように。同じ状況に何度
 * 当たっても機械的に見えないよう、意図してばらつかせてある。`{m}` は該当する
 * ものだけ「あと何分」に置き換わる。
 */
import { 数学, 文字列 } from "../共通/型";
import type { 読み取り専用配列, 省略可, 数値 } from "../共通/型";
import 定型文 from "./定型文.json";

export const 利用制限の文: 読み取り専用配列<文字列> = 定型文.利用制限;
export const 予算超過の文: 読み取り専用配列<文字列> = 定型文.予算超過;
export const 異常の文: 読み取り専用配列<文字列> = 定型文.異常;
export const 拒否の文: 読み取り専用配列<文字列> = 定型文.拒否;
export const 無言の文: 読み取り専用配列<文字列> = 定型文.無言;
/** 作り直しても直前と同じ文しか出てこなかったときの最後の逃げ道。 */
export const 反復打ち切りの文: 読み取り専用配列<文字列> = 定型文.反復打ち切り;
/** 「@kawaiko reset」への返事。設計上トークンを使わない。 */
export const リセットの文: 読み取り専用配列<文字列> = 定型文.リセット;
export const 受け流しの文: 読み取り専用配列<文字列> = 定型文.受け流し;

/** 在庫から 1 つ選ぶ。`{m}` があれば「あと何分」を差し込む。 */
export function 定型文を選ぶ(
  候補: 読み取り専用配列<文字列>,
  変数?: { 分?: 省略可<数値> },
  乱数: () => 数値 = 数学.random,
): 文字列 {
  const 一文 = 候補[数学.floor(乱数() * 候補.length)]!;
  return 一文.replace("{m}", 文字列(変数?.分 ?? 60));
}
