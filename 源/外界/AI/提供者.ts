/**
 * AI の提供者が，置き換え可能な後段として満たすべき形．
 *
 * 提供者は生の文字列を返すだけで，それを kawaiko が実際に投稿してよいかには
 * 口を出さない．漏洩の検査も，無言も，反復も，振る舞い/発話.ts で一度だけ扱う．
 * だから新しい提供者は，存在するだけで全部のガードを継承する．
 */
import type { 発話の依頼, 発話の結果 } from "../../振る舞い/接続口";
import { 文字列, 真偽 } from "../../共通/型";
import type { 約束 } from "../../共通/型";

export interface モデル提供者 {
  /** ログ用の呼び名． */
  readonly 名前: 文字列;
  /** そのモデル id を自分が受け持つか． */
  受け持つか(モデル: 文字列): 真偽;
  実行する(モデル: 文字列, 依頼: 発話の依頼): 約束<発話の結果>;
}
